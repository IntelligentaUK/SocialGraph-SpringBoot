<?xml version='1.0' encoding='UTF-8'?>
<!--
    Minimal Infinispan 15.2 server configuration that exposes the RESP
    (Redis protocol) endpoint on port 11222. Used in the `infinispan-resp`
    docker-compose profile and by `InfinispanRespIntegrationTest` (phase I-B).
    Security realm uses the image's built-in `users.properties`, populated by
    the `USER` / `PASS` env vars on container start.

    RESP commands operate against `respCache`, a simple local cache. In
    production you would promote that to a distributed cache; for RESP-compat
    testing a local cache is sufficient because Lettuce reconnects cleanly.
-->
<infinispan xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xmlns="urn:infinispan:config:15.2"
            xmlns:server="urn:infinispan:server:15.2">

    <cache-container name="default" statistics="true">
        <security>
            <authorization/>
        </security>
        <local-cache name="respCache" statistics="true">
            <!--
                RESP operates on raw bytes at the wire level; Infinispan
                requires the backing cache to declare application/octet-stream
                encoding explicitly (ISPN000964 otherwise).
            -->
            <encoding>
                <key media-type="application/octet-stream"/>
                <value media-type="application/octet-stream"/>
            </encoding>
        </local-cache>
    </cache-container>

    <server xmlns="urn:infinispan:server:15.2">
        <interfaces>
            <interface name="public">
                <inet-address value="${infinispan.bind.address:0.0.0.0}"/>
            </interface>
        </interfaces>

        <socket-bindings default-interface="public" port-offset="${infinispan.socket.binding.port-offset:0}">
            <socket-binding name="default" port="${infinispan.bind.port:11222}"/>
        </socket-bindings>

        <security>
            <security-realms>
                <security-realm name="default">
                    <properties-realm groups-attribute="Roles">
                        <user-properties path="users.properties"
                                         relative-to="infinispan.server.config.path"/>
                        <group-properties path="groups.properties"
                                          relative-to="infinispan.server.config.path"/>
                    </properties-realm>
                </security-realm>
            </security-realms>
        </security>

        <endpoints>
            <endpoint socket-binding="default" security-realm="default">
                <hotrod-connector/>
                <rest-connector/>
                <resp-connector cache="respCache"/>
            </endpoint>
        </endpoints>
    </server>
</infinispan>
